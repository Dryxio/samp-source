# LabelPool New/Draw: one closure, 1034 new code candidates

Normal C++ only, not yet compiled. Nine renderer bodies564 from previous lot are prerequisites, not recounted. Full code union loaded from all four base manifests and logger-queries proof is262111; root reports262513 after pending request/plate402. These four target ranges do not overlap either402 or renderer564. Proposed New11D0/270, Draw1350/643, RemotePlayer IsActive1080/29, TransformLabelPointB4D10/92 sum1034 wholly new code. Original dataset label-pool-original-manifest.json and full captures retained. Complete data ownership must be reconciled separately; accepted_code measures code, not scalar ownership.

Actual complete label record29: text pointer0, ARGBcolor4, VECTORposition8..13, float drawDistance14, bool testLOS18, WORDplayerId19, WORDvehicleId1B. All bytes identified by New stores and Draw consumers, no opaque tail. Pool labels[2048] spanE800 and BOOLslots[2048] follow atE800, whole size10800. Size/offset assertions provided. These records and the complete pool are never newly allocated by the proposal: New allocates only actual text strlen+1 bytes via calloc(1,size), no fictitious shorter label allocation. Existing accepted constructor60/Delete109/destructor44 already own pool lifetime and keep their existing definitions.

New: if id>=2048 return. If old slot nonzero, free old text when present then zero all29 record bytes; no intermediate slot=0 store was present in original. Rotate incoming RGBA intoARGB, allocate strlen+1, copy, invoke real unnamed_100B6100 atB6100 with399 only when copied length<400, then assign every record field and slotTRUE. Preserve lack of allocationfailure guard and preserve real text allocation length (not a guessed400-byte array).

Draw: pGame FindPlayerPed1010, pNetGame pools at3DE, playerpoolslot4/vehiclepoolslot0. Renderer Begin/End guarded exactly as original; draw call itself not given an invented null guard. Every nonempty active label either follows attached player, attached vehicle or fixed world location. Shadow TRUE exactly when alpha=FF. Distances use ordered <=; NaN must skip, as original parity branch does. Player IDs use existing slots bound1004 and actual GetRemote10F0; helperIsActive requires true ped1DD and nonzero byteState10A. Unlike0.2.5 IsActive, R5 does not exclude spectator state explicitly; preserve R5. Remote view is pointer-only with explicit observed state/ped offsets and never allocated.

Player attachment: actual CPlayerPed::GetBonePosition(8,&bone) AE480 already accepted; x/y add recordoffset, z adds recordZ and real GetDistanceFromCamera9F190*0.05f to boneZ. Literal entire float E55F8=CDCC4C3D. Vehicle attachment uses accepted R5VehicleTrailerPoolView GetSlotState1150/GetAt1120 and CEntity GetMatrix9EB10; complete stack matrix64 and offset/output VECTOR12 are real types. New TransformLabelPoint computes each coordinate in exact observed association at.z*point.z + up*point.y + right*point.x + position, preserving per-component loads/stores (no alias-disallow assumption). This independently closes the genuine missingB4D10 provider rather than aliasing D3DX or copying instructions. Fixed labels call actual GetDistanceFromPoint9F2B0 on the local ped. Entity distance9F0C0 already accepted.

Reused declarations: scoreboard query view, spatial renderer complete8 class, trailer-pool view and RPCWorldExtraNetGameView are identical to their actual active provider declarations. CNetGame has no GetVehiclePool method in current header, so actual NETGAME_POOLS field is accessed using existing complete pool descriptor; no fake API. The allocated record/pool views are not used to instantiate alternate game objects.

Sources: closure_label_pool_new_proposed.cpp, closure_label_pool_draw_proposed.cpp, closure_label_pool_helpers_proposed.cpp. Root should probe three independent TUs under existing Ob2 matching profile; helper TU separation preserves original direct IsActive and matrix-transform calls. If generic symbolTransformLabelPoint is already owned elsewhere, reconcile source identity before compiling; no matching active definition was found. No new VM/compiler action performed by agent.

pLabel actual scalar is CLabel* at26EB5C, currently declared in main.cpp45 and assigned startupC4C45 after real label constructor. Reuse actual owner or transfer this sole definition only if root contract requires it; no duplicate scalar file proposed. pGame/pNetGame and renderer/matrix/font owners remain existing. Full font Draw provider6BC90 is the prior exact label body. No new imported provider or unknown native function required, expected EH0. Record1034 as code candidates only; scalar/float data gains not inferred from code union. Strict0.2.5 direct code credit0: labels3D absent source025, active-state source provides context but R5 predicate differs.
