# Matrix rotation72 + ped animation65: independent137-byte proposal

Baseline ped-chain213251; all137 principal bytes are uncovered and distinct from pending1846. Three proposed sources: closure_preview_matrix_rotate_proposed.cpp, closure_store_preview_matrix_proposed.cpp, closure_preview_ped_animation_proposed.cpp. No compiler/VM execution. Manifest contains whole-body hashes, PE relocation offsets and every PE reference into the40-byte native rotation data.

B5790/72 rotates a real caller-supplied MATRIX4X4, never constructs an incomplete native object. It computes axis pointer117400+12*index, sets mutable function pointer117424 to7F1FD0 when iGtaVersion1 else7F2010, then invokes native cdecl RwMatrixRotate(matrix,axis,angle,combine1). C++ address computation and version selection plus original established inlineasm native-call style; index slot reused as pointer as in original. Four pops clean nativeargs and compiler saves/restoresEBX automatically. Actual writable data: three unit axes36 bytes at117400, initial function pointer7F1FD0/4 at117424. Basis vector sources use exact float0/1 payloads; pointer contains nativeVA, not a PE relocation. New real owners only, data excluded from code137.

Primary native identity: Plugin-SDK RenderWare.cpp198–200 gives RwMatrixRotate at7F1FD0 with matrix/axis/floatangle/combine ABI. https://raw.githubusercontent.com/DK22Pac/plugin-sdk/master/plugin_sa/game_sa/RenderWare.cpp . The version offset40 is verified directly from original R5. References B57FB and B583C also use this basis array, confirming utility-global ownership rather than a private fake array solely for the current function.

B1DA0/24 calls real native4D34F0, cdecl RpAnimBlendClumpUpdateAnimations(RpClump*,float step,bool onScreen). Primary Plugin-SDK common.cpp explicitly documents that address/signature: https://github.com/DK22Pac/plugin-sdk/blob/master/plugin_sa/game_sa/common.cpp . R5 wrapper forwards the complete third stack word unchanged; proposed wrapper int onScreen retains this exact forwarding instead of introducing a bool normalization load. The callee's native boolean contract applies to its low byte. No wrapper result is claimed; original discards native EAX. Three argument pops/EBP frame match established bridge style.

AE530/41 is a real pointer-only ped receiver2A4 guard, then existing CEntity::GetRwObject9FA60/12 and nonnull check, then calls that actual B1DA0 wrapper with (clump,step,onScreen), ret8. No invented ped size/allocation. Only new provider dependency for41 is the proposed24, so accept them together. Native4D34F0 is not approximated with BlendAnimation or another similar name.

No EH/tables or new floating literals beyond basis data observed. True existing iGtaVersion26EB28 and GetRwObject9FA60 reused. Full COFF call target and data payload matching still required. Direct0.2.5 byte contribution0: no implementation found there; available native-call style is already in activebase and excluded from direct credit. These137 do not close whole ped/vehicle renderer while constructors, SetMatrixAndUpdate91 and entity Render138 remain unresolved.
