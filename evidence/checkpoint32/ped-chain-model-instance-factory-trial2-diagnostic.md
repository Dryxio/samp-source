# Factory trial2: semantic/native ABI agreement; failure block layout remains

Trial2 object is81 bytes, all three fixups retain identical offsets to original: pGame7, GetModelInfo1C and26. It has the same three locals, two actual lookups, ECX-this native slot2C call and two complete epilogues. The sole structural difference is compiler placement of shared failure block after success rather than before: original failure31..37 then success38..50; trial2 success31..49 then failure4A..50. Corresponding three branch displacements/inversion differ. This is not a target/data mismatch.

The first C++ shape gave79 bytes with merged result reload; early-guard trial2 corrected shared explicit failure epilogue but scheduled it last. No additional grounded C++ form identified that controls this placement without another speculative permutation. Parent explicitly authorized complete symbolic ABI exception after these two bounded attempts. Proposed closure_model_instance_factory_symbolic_proposed.cpp expresses the same two semantic exits with named local slots, actual model-pointer prefix and native vtable slot11; no padding, encoded byte directives, instruction blobs or fake full class.

Root must freshly compile and resolve all81 bytes against B6250. Providers pGame26EBAC and GetModelInfoA7A40 unchanged; PE absolute relocation only7; no new data, EH or table. Native method returns RwObject* inEAX, thisECX, zero explicit arguments, wrappercdecl and savedESI. Proof of actual factory result use and slotidentity remains in model-instance-factory-review.md and original capture. This proposal grants zero coverage pending gate.
