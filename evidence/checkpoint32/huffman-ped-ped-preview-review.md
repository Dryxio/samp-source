# Ped preview634: real allocation and closed dependencies conditional on pending ctor

Source closure_ped_preview_render_proposed.cpp is normal C++ and allocates the actual complete CPlayerPed via its genuine newly declared six-argument constructor. Original requested allocation0x32D, playerNumber208, model0, all coordinates/rotation0. No partial view is allocated; auxiliary views only invoke already implemented nonvirtual methods on the same real object. True thiscall ABI is (int model,DWORD background,VECTOR*rotation,floatzoom), ret16, matching original stack accesses. This preserves the object-preview signature lesson.

Principal6C140/634; expected constructor-exception cleanupE3960/11 calls real operator deleteC648A, handlerE396B/10 reaches CxxFrameHandlerC6526. UnwindMapFB824/8 and FuncInfoFB82C/28. Thus expected newcode655 including21EH, subject to fresh whole compiler evidence. Combined with genuine remote ctor712, useful potential1367 (excluding already pending matrix173/render138 and alreadyaccepted helpers). No fulltextdrawPreviewUpdate closure claimed while vehicle branch remains unimplemented.

Dependencies: ctorB0CE0, nonvirtual SetModelIndexAFF50, accepted Teleport/Gravity/Collision/GetMatrix/SetMatrixAndUpdate, RotatePreviewMatrixB5790, animationAE530→B1DA0, renderer9FB20, complete existing RW helpers and modelreferencecount/release. CPlayerPed vtableED140 already owned: virtual Addslot1→CEntityAdd9F300, Removeslot2→CEntityRemove9F400, deletingslot0→B13D0 and its real destructor/free. These are SA-MP wrapper slots, distinct from native entity slots used inside the utilities. Allocation is before null raster/texture checks as original; original failure-path leaks are preserved, not cleaned up speculatively.

Exact float handling: base height50.05f bits42483333; model162 uses **base height+0.1f**, folding to42489999. Do not replace with50.15f, whose correctly rounded bits4248999A differ by1ULP. This is precisely why checking only inferred relocation targets is insufficient. Teleport Y is zoom*-2.25f, original EA62C bytes000010C0; zero atE5940, animationstep100.0f immediate42C80000. Matrix is explicitly memset64. Root must verify all source payloads and actual instructions, especially folded specialheight, not accept a forced literal alias.

Source provenance: no full0.2.5 preview implementation found. It uses newly source025-informed real pedconstructor and setter, but this655 source-derivedR5 region gets no direct0.2.5 transferbyte attribution. All boundaries/hashes/PEfixups in ped-preview-manifest.json; whole original capture retained.

## Constructor descriptor storage correction before root probe

Use **closure_store_remote_ped_create_player_proposed.cpp** and **closure_store_remote_ped_create_actor_proposed.cpp**, one18-byte actual record perTU. Minimal includes windows.h and game/scripting.h only; no main.h/RakNet headers and their writable sentinel instances. Existing combined closure_store_remote_ped_commands_proposed.cpp is superseded for integration and preserved as proposal history. Source reuses exact real SCRIPT_COMMAND declaration instead of inventing a duplicate struct. These two18-byte records remain the only intended emitted descriptor data owners; inspect COFF sections before gate.
