# Draw662 parked after two motivated C++ trials

Trial1: Draw662 nonmatch, Render202 exact, Update327 nonmatch, menu14 exact.
Trial2: explicit memset corrected actual initial clear; separate true menu TU
restored opaque call boundary and Update336 is exact. Render202/menu14 stay
exact. Draw662 still nonmatch; no speculative third source proposed.

Remaining discrepancy: original prologue PUSH EBP/MOV EBP,ESP/AND ESP,-8/SUB70
and matching EBP epilogues versus compiled SUB ESP,68/ADD ESP,68. All true
observed locals are character64 timestamp, RECTs and integers. No 8-aligned
source object is independently identified. Adding align8 or a dummy double
would merely force shape without evidence, so this is not proposed.
Original loop count initial store occurs after EAX reused; CPP reuses zeroEAX.
Final loop updates original use ECX for next top, CPP EDI and a different load
position for pageSize. Loop alignment bytes also differ as code offsets shift.
Providers, timestamp format and central message rendering semantics correspond;
that does not qualify as an accepted whole match.

Draw662, Render202, Update336 remain a pending1200-byte dependent closure.
Only menu14 can close independently. Do not count getters7/29 solely because
many equivalent bodies exist. Keep all trial captures and sources intact.
