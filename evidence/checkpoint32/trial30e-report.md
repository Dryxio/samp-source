# Trial E: native Luna High, 30-minute research window

Paused. Accepted **+993 unique R5 code bytes**, from419583 to420576/930756 (45.19%). Complete source and linked comparisons, whole data/EH, real COFF/PE targets, required negative controls and ABI checks passed. This is regional coverage, not a whole-DLL match.

At the strict30-minute cutoff: **+496**, or992 bytes/hour. The final lot already engaged before cutoff added497 after validation. Total elapsed including finalization and code commits: **36m11s**, **1646 bytes/hour**. All993 were prepared during the trial; no pretrial candidate credit.

Exactly two native GPT-5.6 Luna High workers with Astra integration. Fast was not guaranteed. Native follow-up worked; no lost CLI steering. Root supplied853 accepted bytes, network worker140, client worker0. Source0.2.5 contributed660 vehicle/damage bytes; existing vendor140; original R5 reconstruction193. The native GTA bridge in the damage implementation retains explicitly documented inline assembly; no copied original instruction bytes or new symbolic adjustor coverage.

Compared with trialD CLI940 bytes/hour and trialC Astra12518 bytes/hour, native orchestration improved on D but remains far below C. These are end-to-end workflow measurements, not a controlled model-speed comparison. No token-price conclusion is supported.

Avoidable integrator mistakes remained: stale coverage ranking caused a zero-gain RPC probe; the rejected scores unit initially duplicated an active owner and failed link. Both costs remain included. Four death-message compiler hypotheses failed; scores had the expected length but wrong complete bytes/targets. Neither receives credit.

Next: use the actual owner index plus accepted RVA union as a mandatory pre-compile filter, and adapt the existing scores owner if pursuing its new binary-grounded hypothesis. Avoid another new strong definition. Death variants and corrected uncompiled scores source remain in ignored build/trial30e. For maximum observed throughput, current evidence favors returning to Astra workers; this trial does not establish an intrinsic model ranking.

Code commits: 14da58d, f376349. Proof: trial30e-two-acceptance.json. No push or GTA deployment. Global checkpoint3.2 remains incomplete and paused.
