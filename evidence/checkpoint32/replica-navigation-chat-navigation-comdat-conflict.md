# Actual link conflict and bounded normal C++ proposal

The linked2 MAP selects closure_gui_widget_lifetime.obj for SDK inline SetTrackPos, VA10026CC0. That true body is23 bytes: it knows Cap preserves ECX because the implementation is visible in that TU. Navigation emitted the original29-byte form, preserving this in ESI across Cap. COFF source fixups14/21 therefore do not describe selected23-byte function fixups11/16. The verifier correctly rejected this COMDAT substitution.

The original R5 helper670E0/29 genuinely preserves ESI; its semantic sequence is position(+8E)=argument, Cap85400, UpdateThumbRect85320. This is not an identity alias to the shortened23-byte provider.

The isolated proposal gives the original29-byte behavior a distinct out-of-line method R5ChatScrollBarNavigationView::R5SetTrackPos. The view derives from the actual CDXUTScrollBar, adds no fields and no virtual functions, uses actual protected m_nPosition/Cap/UpdateThumbRect, and has a sizeof equality assertion. It is never instantiated; existing real scrollbar pointer objects remain owned by their original complete classes. No header modification or force-link order is required. SDK SetTrackPos remains unchanged for all other users.

Only the navigation TU changes. PageUp/PageDown are expected unchanged modulo relocation symbol names; all three bodies still need fresh original whole-body comparison and actual link validation before259 acceptance. No additional bytes beyond the existing259 proposal.
