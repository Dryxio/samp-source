# Trial D — Luna High Fast, then pause

Accepted **+513 unique R5 bytes**, total **419583/930756 (45.079806%)**. Elapsed **32m44s** including setup and finalization: **940.3 bytes/hour**. At the exact30-minute cutoff only316 bytes were accepted (632bytes/hour); the already-started final gate accepted197 more afterward. No push or deployment; checkpoint3.2 remains incomplete.

Two isolated CLI0.153.2 workers used GPT-5.6 Luna, high reasoning, explicit Fast normalized to priority in thread/start configuration. This confirms configuration support, not a measured backend speed multiplier. Root Astra alone changed active files, used VM, reviewed, accepted and committed.

## Coverage and source contribution

- ConnectionRejected284+EH21=305 bytes were prepared before the trial from0.2.5; this trial closed their missing interface adjustment11.
- Fifteen further interface adapters165 are explicit symbolic ABI implementations, each independently equal to genuine MSVC constructor-generated thunks, with exact original interface slots and real accepted callee targets. No constructor/vtable/unimplemented dependency credit.
- Five normal vendor client wrappers25 and actual MTUgetter7 add32. Another54 historical provider bytes were implemented in the active capsule but deduplicated to zero gain. Network worker proposed four of these final wrappers; root corrected active ownership, added real providers and split translation units.
- Client worker:0 accepted bytes. Death-message candidate490 vs493 remains rejected. A one-byte fallback was prepared but not compiled or accepted.

## Outcome

This end-to-end arrangement is substantially below trial C's12517.8bytes/hour. It does not isolate Luna's ability: new CLI orchestration setup, candidate selection, and integrator failures are confounders. Three steering requests sent after the client turn finished were rejected; root failed to restart it promptly. Historical coverage was also mistaken for available active ownership. These are real costs in this result, not excluded setup time.

Required whole-source/linked function,data,EH comparisons, every relocation/actual target, negative controls and ActorABI all passed for both accepted lots. No masking, truncation, copied original instruction bytes, fake provider or duplicate credit.

## Next

Before another speed trial, make the broker process responses and completed turns automatically, restart idle workers when a followup exists, and enforce symbol+active owner+complete RVA dependency preflight. Do not use this experimental CLI broker unchanged for overnight work. The rejected493-byte death-message root has an untested integrator hypothesis: copy decoded address-taken IDs into fresh non-address-taken WORD locals; original preserves IDs/array element addresses across calls. Network Update/SendBitStream remain blocked by actual GenerateDatagram/SendTo providers and unresolved output-buffer extent. No new025 contribution was accepted beyond the earlier305-byte candidate unlocked here.

Detailed per-worker token counts, durations, timed phases and errors: trial30d-results.json. Cached input is counted repeatedly by CLI and is not a monetary-cost estimate. Original binary, objects, raw broker events and disassembly remain in ignored private/build directories. Preexisting main.cpp modification is preserved.
