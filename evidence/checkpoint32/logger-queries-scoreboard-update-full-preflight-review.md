# Scoreboard Update + queries: complete scoped preflight

Actual objects: cp32-scoreboard-update-trial3/closure_scoreboard_update.obj and previously fully reviewed cp32-scoreboard-update-trial2/closure_scoreboard_player_queries.obj. Full manifest scoreboard-update-reviewed-manifest.json resolves all 28 Update COFF bindings to actual provider symbols, includes raw COFF addends in target_va, matches all892 bytes and compares complete PE relocation site sets both ways. Five query bodies432 are also whole and have no fixups/relocations. EH0. Two complete format string sections3+3 are checked byte-for-byte at E9344 (`%u` + NUL) and EA6AC (`%d` + NUL); gate decides unique data gain, not added to1324 potential code.

Providers: actual slot/user count139F0, NPC E470, ping6E880, score6E850 and remote10F0 identities follow the real original Update callsites, documented in scoreboard-player-queries-reviewed-manifest.json. CNetPlayer fields score4/NPC8/pingC/remote10 are from real current header and original offsets, never an allocated fake class. pNetGame already owned; GetPlayerName actual RPCDeathPlayerPoolView method175C0 reused unchanged; actual local and remote ARGB getters reused. CDXUT list methods and scrollbar Cap/UpdateThumbRect all resolve to existing reviewed contract providers. Real ListBox AddItem254/SetColumn106 resolve to the now accepted complete-item reconstruction. No CChatWindow or unrelated alias substitution.

Allocated sortable record is a complete packed45 bytes: color0, name4[29], id21, score25, ping29. calloc(playerCount,45) matches original; no artificial failure check. Whole record swapping retains all45 bytes, existing std::string representation is accessed through actual existing GetLocalPlayerName/c_str. ListBox owner view is never allocated and new items are allocated only by the separately reviewed complete29E-byte provider.

Scratch buffer source reconstruction is nonunique: original physical region ESP+60 through ESP+163 is260 bytes, capacities257..260 map to that same observed frame. Chosen buffer260 names precisely that real region; no dummy variable, struct hole, forced alignment, copied bytes, masks or changed call semantics. See scoreboard-update-buffer-extent.md. Original and trial3 now match all frame and live local bytes, rather than ignoring a frame mismatch.

Direct0.2.5 source credit0; reconstruction uses base/current R5-specific player/list APIs and full original layout. Exclude unreferenced compiler helpers/sentinels and header data. Static preflight is not acceptance; root still owns linked verification, unique union and final gate.
