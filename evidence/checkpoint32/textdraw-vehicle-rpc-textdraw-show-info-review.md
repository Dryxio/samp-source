# ShowTextDraw + true AddInfoMessage, normal C++

Stable proposals closure_rpc_show_textdraw_proposed.cpp and closure_chat_info_proposed.cpp; no active header edits. CChatWindow currently declares AddDebugMessage,AddEntry,Log but not AddInfoMessage. Its existing private info-color member is offset126 (debugColor12A). A new pointer-only R5ChatInfoView declares the actual variadic info function and reads the real infoColor126. It casts this to actual CChatWindow only to call its accepted AddEntry. No reinterpretation of AddDebug as info, no whole-class allocation/size claim or conflicting CChatWindow redeclaration. Both proposalTUs contain the same InfoView declaration.

Actual AddInfoMessage68070/117 formats into zeroed512-byte buffer using vsprintfCA304, sanitizes positive signedchars below32 tospaces, calls true AddEntry67BE0(type4,buffer,null,infoColor126,0). AddDebug680F0/117 uses type8 and debugColor12A: same length, semantically distinct. Variadic C++ member uses cdecl ABI, this on stack, caller pops all arguments; the Show call pushes promoted unsigned16-bit length, format, this and cleans12, agreeing with original1CE40..1CE4F. Compiler-target review must preserve exact providers, not infer by raw similarity alone.

Show1CD90/297 reads ID16, full R5 transmit63, length16 into zeroedDWORD. At length>=800 it conditionally reports the precise warning via Info; otherwise reads that many bytes, writes terminator and calls actual PoolNew1E910. Opposite Edit bound (>800 rejects) deliberately not copied. Uses char* BitStream constructor1F840. Zeroedlength declaration is placed before pool extraction, matching original and the just-observed Edit diagnostic. No new return-value or network null checks.

EH21 E14F0 comprises stream-unwind11 + handler10 E14FB. FuncInfoF8FBC and unwindmapF8FB4 total36; stream atEBP-440. Actual ctor/read/dtor providers accepted, PoolNew already exact linked in parentconstruction lot. Warning literalE6404 complete contents/size/hash in manifest. AddInfo itself has no EH/table attachment. No extra globals; pChatWindow andpNetGame are existing owners.

Source025 supplies complete AddInfo behavior, but current accepted base AddDebug already supplies essentially the whole formatting/sanitizer implementation, with only type/color distinction. Strict additive025 credit for helper is therefore0 (do not reclassify existing base behavior as new025 gain). Show skeleton comes from025, but R5 variablelength/payload63/warning control flow are missing from025: no full297 direct025 credit. Qualitative reference corroboration is useful; candidate435 wholebytes would count as uniqueR5 code after acceptance, with zero new direct025 credit under strict attribution.
