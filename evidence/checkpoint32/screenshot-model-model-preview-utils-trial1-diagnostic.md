# Bounded trial1 diagnostic

Radius41 vs74 and centre60 vs93: each is shorter by33. Same two actual lookupcalls and nativefield offsets; optimizer removed exactly the two source pointerlocals/zero-init reload sequence and arranged successbeforefailure. Original reserves8 bytes, stores modelpointer atESP+8, zeros collision atESP+4, reloads model, reads collision14, stores/reloads collision then tests. Original failureepilogue precedes final positive return. Thus one normal C++ trial2 is motivated: local pointers explicitly volatile to retain original observed stack accesses, take one final nonvolatile snapshot to avoid extra reload, use earlyfailure returns. No native object fields volatile or semantics changed; only private automatic pointerlocals. All source failures preserved. Must reject if emittedaccesses/branches differ; no blind further volatile variants.

RenderAtomic41 vs59 is different: original EBPframe/local4 saves queried callback then inline native callback bridge explicitly usesEBX and restores it, cleanup onepush viaPOPEBX, ending stdcallret4. Normal functionpointer C++ picksESI/EAX, noframe and batches8-byte cdeclcleanup, semantically same but nonmatching. Merely making callbackvolatile cannot justify all original EBX/frame/cleanup choices. No further arbitrary C++ register variable permutation proposed. Small explicit symbolic ABI bridge in otherwise normal C++ after this failed normaltrial would reflect original and existingbase nativecallback idiom, but needs rootdecision before new implementation. Four exact functions9/20/37/57 retain originalsource; RenderRw37 remains dependencyblocked until true RenderAtomic59 exact.
