Trial4 source1308 matches every instruction/register/branch topology after symbol normalization except FADD operand width at function+1F2: source qword (__real@3fd0000000000000, bytes000000000000d03f) versus original dword EC314 (0000803e). No other true instruction differences. Do NOT bind this8-byte symbol to original4-byte storage. The double conditional common-type hypothesis was correct. Final C++ refinement keeps double conditional result but computes true addition with float literal: condition ? (double)(z+0.25f) : (double)z. This targets only operand-width/type; no structural/register permutations. Requires full original payload and every relocation comparison after compilation.
